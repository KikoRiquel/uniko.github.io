---

---

<nav>
    <a href="/" class="nav--logo"
        ><img src="/assets/logo/logo_uniko.svg" alt="" /></a
    >
    <a href="/emile" class="nav--emile">emile</a>
    <a href="/pascale" class="nav--pascale">pascale</a>
</nav>

<style>
    nav {
        position: fixed;
        top: 2rem;
        left: 0;
        right: 0;
        z-index: 1000;
        padding: 0 var(--default-spacing-x);

        display: grid;
        grid-template-columns: var(--grid-container-columns);
        grid-template-rows: auto;
        gap: var(--grid-container-gap);

        height: min-content;
    }

    .nav--logo {
        width: 100%;
    }

    .nav--emile {
        grid-column: 1;
        grid-row: 2;
    }

    .nav--pascale {
        grid-column: 1;
        grid-row: 3;
    }
</style>
